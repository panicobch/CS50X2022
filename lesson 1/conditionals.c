#include <stdio.h>

int main (void)
{
    int x = 10;
    int y = 0;
    if (x > y)
    {
        printf("X is bigger than Y\n");
    }
    else
    {
        printf("Y is bigger than X\n");

    }
}